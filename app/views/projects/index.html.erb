<div>
    <table>
        <thead>
        <th>UUID</th>
        <th>Name</th>
        <th>Description</th>
        <th>&nbsp;</th>
        </thead>
        <% for project in @projects %>

            <tr>
                <td><%= project.uuid %></td>
                <td><%= project.name %></td>
                <td><%= project.description %></td>
                <td><%= link_to "Edit", edit_project_path(project.id) %></td>
                <td><%= link_to "View", project_path(project.id) %></td>
                <td><%= link_to "Review", project_review_path(project.id) %></td>
            </tr>

        <% end %>
    </table>
</div>
<div>
    <a href=<%= new_project_path %>>New Project</a>
</div>